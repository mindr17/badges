import s from './selected.module.css';

export default function BadgeProduct(): JSX.Element {
  return (
    <div className={s.container}>
      <h2 className={s.description}>Selected badges</h2>
      <div className={s.description}>Selected badges</div>
    </div>
  );
}
